import { LineBreakPipe } from './line-break-pipe.pipe';

describe('LineBreakPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
